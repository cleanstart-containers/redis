# Sample Dockerfile: use cleanstart/redis:latest-dev as base
# Build: docker build -t my-redis .
# Run:   docker run -d -p 6379:6379 my-redis

FROM cleanstart/redis:latest-dev

# Optional: add custom config or scripts
# COPY redis.conf /etc/redis/
# USER 1000:1000

EXPOSE 6379
